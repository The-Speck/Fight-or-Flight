import Plane from './plane.js';

class User {
  constructor({ctx, canvas}) {
    const userOptions = {
      type: 0,
      ctx,
      pos: [canvas.width/2, canvas.height/2],
      vel: [1, 1]
    };
  }

  
}

export default User;
